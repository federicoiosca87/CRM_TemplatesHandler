# This file is managed by `bego secrets` and MUST NOT be updated manually.
# Run `bego secrets set <env> <key> <value>` to add or rotate secrets.
# Required env vars (injected at runtime by docker-entrypoint.sh):
#   OAUTH_AUTHORITY, OAUTH_CLIENT_ID, OAUTH_CLIENT_SECRET, OAUTH_SCOPES, OAUTH_REDIRECT_URI

appName: crm-templates-handler
group: campaigns-rewards
secrets:
    test:
        global: {}
    qa:
        global: {}
files: {}
namespaceSecrets: {}
namespaceFiles: {}
