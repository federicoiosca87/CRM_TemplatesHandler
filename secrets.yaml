# This file is managed by `bego secrets` and MUST NOT be updated manually.
# Run `bego secrets set <env> <key> <value>` to add or rotate secrets.
#
# OAuth configuration is NOT stored here. It is resolved at runtime by
# oauth_config.py using the ENVIRONMENT env var injected by bego (or APP_ENV
# for direct Docker builds). No OAUTH_* secrets are needed in this file.

appName: crm-templates-handler
group: campaigns-rewards
secrets:
    test:
        global: {}
    qa:
        global: {}
files: {}
namespaceSecrets: {}
namespaceFiles: {}
